# Data

This directory is for local and server runtime data, not source code.

- `raw/`: raw external data snapshots
- `processed/`: processed data artifacts
- `exports/`: exported reports or JSON files
- `runtime/logs/`: backend pipeline logs
- `runtime/state/`: scheduler and pipeline state files

Runtime contents are ignored by Git.
